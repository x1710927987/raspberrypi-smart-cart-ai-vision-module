version: v0
mode: auto

camera:
  device_index: 0
  width: 640
  height: 480
  fps: 20

serial:
  port: COM3
  baudrate: 115200
  timeout_sec: 0.2
  heartbeat_hz: 5

runtime:
  target_fps_min: 10
  safety_brake_on_lost_ms: 300

logging:
  level: INFO
  save_frames: false
  log_dir: runs/logs
