runtime:
  target_fps_min: 10.0
  safety_brake_on_lost_ms: 300
  mock_mode: true

perception:
  camera_index: 0
  device: "cpu"

control:
  serial_port: "/dev/ttyUSB0"
  baud_rate: 115200

serial:
  port: "/dev/ttyUSB0"
  baud: 115200

logging:
  level: "DEBUG"
  log_dir: "logs"
